---
const { rootUrl, userName, platformName } = Astro.props;
---

<div>
    <a href={`${rootUrl}${userName}/`}>{platformName}</a>
</div>

<style>
    a {
        font-size: 0.8rem;
        padding: 0.3rem 0.5rem;
        color: #555; /* 背景色だった暗いグレーを文字色に */
        background-color: #f7f7f7; /* 文字色だった明るい色を背景色に */
        text-decoration: none;
        border-radius: 4px; /* 角を丸めて柔らかい印象に */
    }

    a:hover,
    a:focus {
        background-color: #ddd; /* ホバー時の背景色を少し暗く */
        color: #333; /* ホバー時の文字色を少し濃く */
    }
</style>
